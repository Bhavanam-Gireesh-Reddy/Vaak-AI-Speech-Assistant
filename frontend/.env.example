# Server-side backend URL (used by Next.js API proxy routes)
BACKEND_URL=http://127.0.0.1:8000

# Client-side backend URL (used by WebSocket in the live page)
# On Render: set to https://meetwise-api.onrender.com
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
